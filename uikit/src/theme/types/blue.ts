import { extendStandard } from './extend';

export const Blue = extendStandard({
  fonts: {
    heading: 'Helvetica',
    body: 'Inter, system-ui, Avenir, Helvetica, Arial, sans-serif',
  },
  colors: {},
});
