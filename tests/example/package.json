{
    "name": "ts-retype-example",
    "version": "1.0.0",
    "description": "",
    "license": "ISC",
    "author": "",
    "main": "src/index.ts",
    "type": "module",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist && rm -rf node_modules/ts-retype",
        "report": "ts-retype .",
        "smoke": "tsc && npm run report && npm run start && npm run ts -- -- src/smoke.ts",
        "start": "npm run ts -- -- src/index.ts",
        "ts": "node --experimental-specifier-resolution=node --loader ts-node/esm"
    },
    "dependencies": {
        "ts-retype": "^0.3.0-dev.5"
    }
}
